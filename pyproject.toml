[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "thanatos_intel"
version = "0.0.1"
description = "Intelligence Investigation Platform MVP"

[tool.setuptools]
include-package-data = true

[tool.setuptools.packages.find]
where = ["."]
include = ["thanatos_intel*"]
